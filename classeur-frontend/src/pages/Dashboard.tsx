export default function Dashboard() {
    return (
        <div className="dashboard">
            <h1>Tableau de bord</h1>
            <p className="stub-page__note">
                Les compteurs (personnes, sources, documents, correspondances, doublons…) s'activeront
                à partir de la phase M1 (synchronisation en lecture seule de DGhubschool).
            </p>
        </div>
    );
}
